<%= form_with(model: activity_type, local: true) do |form| %>
  <% if activity_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(activity_type.errors.count, "error") %> prohibited this activity_type from being saved:</h2>

      <ul>
        <% activity_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :fa_icon %>
    <%= form.text_field :fa_icon %>
  </div>

  <div class="field">
    <%= form.label :alt_icon %>
    <%= form.text_field :alt_icon %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="field">
    <%= form.label :intensity %>
    <%= form.text_field :intensity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
